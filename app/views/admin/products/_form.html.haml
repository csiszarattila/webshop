#left-column
  %fieldset
    %legend Általános jellemzők
    .input
      %label{ :for => "name"} Név
      =f.text_field :name
    .input
      %label{ :for => "category" } Kategória
      =select_tag "product[category_id]", product_category_options_for_select(@root_categories,@product.category)
    .input
      %label{ :for => "price" } Ár 
      =f.text_field :price
    .input
      %label{ :for => "description" } Leírás
      =f.text_area :description, :size => "60x20"
  %fieldset
    %legend Termék jellemzők
    -if @product.category_attributes.nil?
      %p A termék kategóriájához nem adtak meg jellemzőket.
    -else
      - @product.attrs.each do |attribute|
      - f.form_for :attrs, attribute do |attribute_form|
        .input
          = attribute_form :name
          = attribute_form :value
  %fieldset#new-picture 
    %legend Új kép csatolása
    %input{:type=>"file",:name=>"new_picture"}
#right-column
  %fieldset#pictures
    %legend Kapcsolódó képek
    = render :partial => 'admin/products/image_on_edit.html.haml', :collection => @product.images, :as => :image, :locals => { :product => @product }